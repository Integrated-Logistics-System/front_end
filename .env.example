# 🚀 Recipe AI Frontend 환경설정

# 프로덕션 환경 (Docker + Nginx 프록시)
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_WS_URL=/ws

# 백엔드 호스트 (Docker 배포 시)
BACKEND_HOST=host.docker.internal

# 개발 환경에서의 직접 연결 설정 
# NEXT_PUBLIC_API_URL=http://localhost:8081/api    # 직접 연결
# NEXT_PUBLIC_WS_URL=http://localhost:8083         # 직접 연결