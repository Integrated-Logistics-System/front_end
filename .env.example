# 🚀 Recipe AI Frontend 환경설정

# 백엔드 API URL (개발환경)
NEXT_PUBLIC_API_URL=http://localhost:8081/api

# WebSocket URL (개발환경)  
NEXT_PUBLIC_WS_URL=ws://localhost:8083

# 백엔드 호스트 (Docker 배포 시)
BACKEND_HOST=host.docker.internal

# 프로덕션 환경에서의 백엔드 설정 
# BACKEND_HOST=192.168.0.111  # Synology NAS IP
# NEXT_PUBLIC_API_URL=/api    # Nginx 프록시 사용
# NEXT_PUBLIC_WS_URL=/ws      # Nginx 프록시 사용