# 🚀 Recipe AI Frontend 환경설정

# ============================================
# 프로덕션 환경 (Docker + Nginx 프록시)
# ============================================
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_WS_URL=/ws

# 백엔드 호스트 (Docker 배포 시)
BACKEND_HOST=host.docker.internal

# Node 환경
NODE_ENV=production

# ============================================
# 사용법:
# ============================================
# 개발: .env.dev 파일 사용
# 프로덕션: 이 파일(.env.example)을 .env.local로 복사하여 사용