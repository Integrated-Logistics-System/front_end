# 🛠️ Recipe AI Frontend - 개발 환경 설정

# ============================================
# 개발 환경 (로컬 백엔드 직접 연결)  
# ============================================
# next.config.mjs의 기본값(상대경로)을 절대경로로 오버라이드
NEXT_PUBLIC_API_URL=http://localhost:8081/api
NEXT_PUBLIC_WS_URL=http://localhost:8083

# Node 환경 
NODE_ENV=development

# 텔레메트리 비활성화 (선택사항)
NEXT_TELEMETRY_DISABLED=1

# 개발용 디버깅 플래그 (선택사항)
# DEBUG=true
# NEXT_DEBUG=true

# ============================================
# 참고: next.config.mjs의 기본값
# ============================================
# NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL || '/api'
# NEXT_PUBLIC_WS_URL: process.env.NEXT_PUBLIC_WS_URL || '/ws'
#
# 이 파일의 환경변수가 next.config.mjs의 기본값을 오버라이드함