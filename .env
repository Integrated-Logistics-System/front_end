# 기본 환경 설정 (.env)
# 모든 환경에서 공통으로 사용되는 기본값

# 기본 텔레메트리 비활성화
NEXT_TELEMETRY_DISABLED=1

# 기본 타임아웃 설정
API_TIMEOUT=600000

# 기본 WebSocket 설정
NEXT_PUBLIC_WS_PING_INTERVAL=30000

# 보안 설정
NEXT_PUBLIC_SESSION_TIMEOUT=86400000

# 기본 UI 설정
NEXT_PUBLIC_ANIMATION_DURATION=300